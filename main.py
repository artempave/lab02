# This is a sample Python script.
from Struct.Currency import Currency
from Library.LibTerminal import Terminal
from Clients.ClientRecvTranslateCur import ClientRecvTranslate

# Press ⌃R to execute it or replace it with your code.
# Press Double ⇧ to search everywhere for classes, files, tool windows, actions, and settings.

if __name__ == '__main__':
    #RUB = Currency("RUB", 2.4)
    #print(RUB)

    #_ClientRecvTranslate = ClientRecvTranslate()
    #_ClientRecvTranslate.exchange_currency("USD", "RUB", "1")

    terminal = Terminal()
    terminal.start()
    #with open("users.txt", "w") as file:
        #file.write("привет")
# See PyCharm help at https://www.jetbrains.com/help/pycharm/
